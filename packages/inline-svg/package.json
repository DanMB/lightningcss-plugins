{
	"name": "lightningcss-plugin-inline-svg",
	"version": "0.0.1",
	"description": "Compile SVG files as inline URL(), with the ability to add a specific color",
	"keywords": [
		"lightningcss-plugin"
	],
	"repository": {
		"type": "git",
		"url": "https://github.com/danmb/lightningcss-plugins",
		"directory": "packages/inline-svg"
	},
	"type": "module",
	"files": [
		"index.js",
		"index.d.ts"
	],
	"scripts": {
		"format": "prettier --check --write .",
		"lint": "eslint",
		"check": "tsc  --project tsconfig.json --noEmit",
		"build": "tsc --project tsconfig.json"
	},
	"dependencies": {
		"svgo": "^3.3.2"
	},
	"peerDependencies": {
		"lightningcss": "^1.10.0"
	}
}
